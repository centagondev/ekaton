# Copy to .env and adjust if the backend runs elsewhere.
VITE_API_URL=http://localhost:8000/api/v1
# WebSocket origin. If omitted it is derived from VITE_API_URL (http -> ws).
VITE_WS_URL=ws://localhost:8000
